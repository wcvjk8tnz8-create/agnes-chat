# 让 npm 在 peer 依赖版本不一致时仍然安装，而不是直接报 ERESOLVE 中断构建。
#
# 背景：本项目同时存在两组常见的 peer 冲突——
#   1. Radix UI 各包的 peer 仍写着 react ^18，而 Next 16 要求 react 19.2
#   2. wrangler 4.x 的 peerOptional 与部分 Cloudflare 类型包版本不同步
# 这些包在运行时都能正常工作（Radix 1.x 实际兼容 React 19），
# 但 npm 默认会把 peer 不匹配当成致命错误，导致 Vercel / Actions 安装阶段直接失败。
#
# 直接依赖（next / react / react-dom / @opennextjs/cloudflare / wrangler）
# 的版本都在 package.json 里锁着，peer 只影响传递依赖，跳过检查是安全的。
legacy-peer-deps=true
