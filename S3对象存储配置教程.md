# S3 对象存储配置教程

> 配好之后，聊天里就能传图片和视频，AI 也**看得见**图片内容。
> 不配也能正常聊天，只是不能上传文件。

本文以 **Backblaze B2** 为主线（Vercel / Netlify 部署的推荐方案），
同时给出 MinIO、AWS S3、阿里云 OSS、腾讯云 COS、七牛的填法。

> **用 Cloudflare Workers 部署？** 不需要 AK/SK，看
> [R2 对象存储配置教程](./R2对象存储配置教程.md) 更省事。

---

## 零、先选：两种配法

| | 环境变量（推荐站长用） | 设置面板里手填 |
|---|---|---|
| 谁来配 | 站长，在部署平台填一次 | 每个用户自己填 |
| 用户要不要管 | **不用**，打开就能传 | 要自己去设置里填 |
| 密钥在哪 | 只在服务端 | 存在浏览器本地 |
| 适合 | 公开站点 | 自己一个人用、或临时试 |

**推荐环境变量**：用户不用管，密钥也不下发到浏览器。

---

## 一、该选哪家？

| 部署平台 | 推荐 | 原因 |
|---|---|---|
| **Cloudflare Workers** | **Cloudflare R2** | 零出站流量费，有 binding 不用密钥 |
| **Vercel / Netlify** | **Backblaze B2** 或 **Supabase** | B2：10GB 免费存储；Supabase：1GB 存储但自带 CDN，且后台统一管理 |
| **已在用 Supabase 数据库** | **Supabase Storage** | 一个后台管数据库和存储，不用再多注册一家 |

---

## 二、Backblaze B2 完整流程

### 第 1 步：注册

打开 [backblaze.com](https://www.backblaze.com/b2/) 注册账号。

> 首次登录会要求验证邮箱，还会提示开启两步验证（建议开）。

### 第 2 步：创建桶

1. 左侧菜单 → **B2 Cloud Storage** → **Buckets**
2. 点 **Create a Bucket**
3. 填写：

| 项 | 填什么 |
|---|---|
| Name | `agnes-chat`（**全局唯一**，被占了就加后缀，如 `agnes-chat-hk`） |
| Files in Bucket are | **Public**（要让 AI 和外链能访问） |
| Encryption | 默认（Disable 即可） |
| Object Lock | 关 |

4. 点 **Create a Bucket**

创建完，**记下 Endpoint 里的那串区域名**，比如 `us-west-004`。

### 第 3 步：拿到密钥

1. 左侧菜单 → **Account** → **Application Keys**
2. 点 **Add a New Application Key**
3. 填写：

| 项 | 填什么 |
|---|---|
| Name | `agnes-chat` |
| Allow access to Bucket | 选刚建的那个桶 |
| Type of Access | **Read and Write** |

4. 点 **Create New Key**

**⚠️ 只看得到一次**，立刻复制：

| 字段 | 对应环境变量 |
|---|---|
| `keyID` | `B2_ACCESS_KEY_ID` |
| `applicationKey` | `B2_SECRET_ACCESS_KEY` |

> 关掉就再也看不到了，只能重新生成。

### 第 4 步：确定公开访问域名

B2 给两种外链格式，二选一：

| 格式 | 样子 | 在哪看 |
|---|---|---|
| **S3 兼容**（推荐） | `https://agnes-chat.s3.us-west-004.backblazeb2.com` | 自己拼：桶名 + 区域 |
| **原生友好 URL** | `https://f005.backblazeb2.com/file/agnes-chat` | 桶详情页的 **Friendly URL** |

把选好的填进 `B2_PUBLIC_BASE_URL`，**末尾不要带斜杠**。

### 第 5 步：配 CORS（浏览器直传必需）

浏览器直传文件到 B2，不发 CORS 头的话会被拦。

在桶详情页 → **Bucket Settings** → **CORS Rules**，选 **Share everything in this bucket with all origins**。

> 想收紧就手填规则：
>
> | 项 | 值 |
> |---|---|
> | Origin | 你的站点域名（如 `https://agnes-chat.vercel.app`） |
> | Allowed Operations | `PUT`、`GET` |
> | Max Age | `3600` |

⚠️ **B2 的 S3 兼容层不支持 `PutBucketCors`**，所以只能用后台界面配，不能用命令行或 SDK 设。

### 第 6 步：填环境变量

在部署平台（Vercel / Netlify）的 **Environment Variables** 里加：

| 变量名 | 值 |
|---|---|
| `B2_REGION` | `us-west-004`（第 2 步记下的） |
| `B2_ACCESS_KEY_ID` | 第 3 步的 `keyID` |
| `B2_SECRET_ACCESS_KEY` | 第 3 步的 `applicationKey` |
| `B2_BUCKET` | `agnes-chat` |
| `B2_PUBLIC_BASE_URL` | 第 4 步的域名 |

前四个必填，缺任何一个都不启用。

> **改完要重新部署一次**才生效。

---

## 二·五、Supabase Storage（推荐给已在用 Supabase 的人）

Supabase Storage **本身就是 S3 兼容的**，走 `/storage/v1/s3` 端点。
如果你已经在用 Supabase 当数据库，存储也放这儿最省事 —— 一个后台管全部。

### 免费额度

| 项 | 免费版 |
|---|---|
| 存储 | **1 GB** |
| 出站（Cached Egress） | 5 GB/月 |
| **单文件上限** | **50 MB** |

> ⚠️ 单文件 50MB 是硬限制。本项目视频上限 100MB，
> **超过 50MB 的视频会上传失败**，这是 Supabase 免费版的限制，不是配置问题。
> 要传大视频就用 B2 或 R2。

### 第 1 步：启用 S3 并生成密钥

1. 打开 Supabase 后台 → 你的项目
2. 左侧 **Storage** → **S3 Configuration**
3. 确认 S3 连接已启用（Enable S3 connection via S3 protocol）
4. 点 **Access keys** → **New access key**
5. **立刻复制**（只显示一次）：

| 页面上的字段 | 对应环境变量 |
|---|---|
| Access Key ID | `SUPABASE_ACCESS_KEY_ID` |
| Secret Access Key | `SUPABASE_SECRET_ACCESS_KEY` |
| Region | `SUPABASE_REGION` |
| Endpoint 里的 `<ref>` | `SUPABASE_PROJECT_REF` |

> 页面上还会直接给出 endpoint 和 region，**以页面为准**。

### 第 2 步：创建桶

1. 左侧 **Storage** → **New bucket**
2. 名字填 `uploads`（**建议别用 `agnes-chat`**，理由见下方说明）
3. **Public bucket** 打开 —— 否则外链访问不到，AI 也看不到图
4. 点 **Create bucket**

> **为什么建议 `uploads`**：公开外链格式是
> `.../object/public/<桶名>/<目录前缀>/<文件名>`，
> 而目录前缀默认就是 `agnes-chat`。
> 桶名也叫 `agnes-chat` 的话，URL 里会出现两次，看着像配置错误
> （实际能用，只是容易误判）。

### 第 3 步：填环境变量

| 变量名 | 值 | 必填 |
|---|---|---|
| `SUPABASE_PROJECT_REF` | 项目 ref，如 `abcdefghijklmnop` | ✅ |
| `SUPABASE_ACCESS_KEY_ID` | 第 1 步的 Access Key ID | ✅ |
| `SUPABASE_SECRET_ACCESS_KEY` | 第 1 步的 Secret Access Key | ✅ |
| `SUPABASE_BUCKET` | `uploads` | ✅ |
| `SUPABASE_REGION` | 项目区域，如 `us-east-1` | 留空默认 `us-east-1` |
| `SUPABASE_PUBLIC_BASE_URL` | 自定义公开域名 | 留空自动拼 |

**endpoint 和公开域名都自动拼**，你不用手抄：

```
上传端点：  https://<ref>.storage.supabase.co/storage/v1/s3
公开域名：  https://<ref>.supabase.co/storage/v1/object/public/<bucket>
```

> 上传端点用的是 `storage.supabase.co` 直连主机名，
> 官方文档说大文件上传性能更好。

改完**重新部署一次**才生效。

### ⚠️ 两个必须知道的限制

**① 只接受 path-style，且已自动处理**

Supabase 的 S3 兼容层**只认 path-style**
（`https://endpoint/<桶名>/<文件路径>`），
不接受 virtual-host（`https://<桶名>.endpoint/...`）。

代码里已经加了 `supabase.co` 的自动识别，
**你不用手动设 `S3_FORCE_PATH_STYLE`**。

**② 不支持 PutBucketCors**

S3 兼容层**没有实现 bucket CORS 相关接口**，
所以不能用命令行或 SDK 配 CORS。

托管版 Storage 默认允许跨域，一般没问题。
万一遇到 CORS 报错，说明该项目的策略较严 ——
这种情况建议改用 B2 或 R2。

### 其他注意

- **S3 兼容层目前是 public alpha**，接口可能变；出问题先查
  [官方兼容性文档](https://supabase.com/docs/guides/storage/s3/compatibility)
- 不支持的操作：bucket CORS、lifecycle、encryption、ACL 头、object lock、checksum 头
- **S3 协议和 REST API 互通**：用 S3 传上去的文件，
  能用 REST 公开 URL 正常访问，所以外链没问题
- 自带的**图片变换**（缩放、裁剪）需要 Pro 版，免费版没有

---

## 三、其他 S3 兼容服务

不想用 B2 就填通用变量 `S3_*`：

| 变量名 | 说明 |
|---|---|
| `S3_BUCKET` | 桶名 |
| `S3_ENDPOINT` | 服务端点（**必须带 https://**） |
| `S3_REGION` | 区域，多数填 `auto` 或 `us-east-1` |
| `S3_ACCESS_KEY_ID` | Access Key |
| `S3_SECRET_ACCESS_KEY` | Secret Key |
| `S3_PUBLIC_BASE_URL` | 公开访问域名（可选） |
| `S3_FORCE_PATH_STYLE` | 寻址风格，`true` / `false`，一般不用填 |

### 各家怎么填

| 服务 | Endpoint | Region |
|---|---|---|
| **Supabase** | `https://<ref>.storage.supabase.co/storage/v1/s3` | 项目区域 |
| **AWS S3** | `https://s3.us-east-1.amazonaws.com` | `us-east-1` |
| **MinIO 自建** | `https://minio.example.com:9000` | `us-east-1` |
| **阿里云 OSS** | `https://oss-cn-hangzhou.aliyuncs.com` | `oss-cn-hangzhou` |
| **腾讯云 COS** | `https://cos.ap-guangzhou.myqcloud.com` | `ap-guangzhou` |
| **七牛云** | `https://s3-cn-south-1.qiniucs.com` | `cn-south-1` |

> 阿里云 OSS 和七牛的 S3 兼容层**只覆盖部分操作**，
> 如果上传失败，换回各家原生 SDK 对应的设置或改用 B2。

---

## 四、寻址风格（path-style vs virtual-host）

这是最容易踩的坑。两种 URL 拼法：

```
path-style:     https://endpoint/桶名/文件名
virtual-host:   https://桶名.endpoint/文件名
```

**系统会自动判断**，一般不用管：

| 服务 | 自动选 |
|---|---|
| R2、MinIO、**Supabase**、内网 IP、localhost | path-style |
| AWS S3、B2、阿里云 OSS | virtual-host |

**判断错了才需要手动覆盖**：

```bash
S3_FORCE_PATH_STYLE=true    # 一律 path-style
S3_FORCE_PATH_STYLE=false   # 一律 virtual-host
```

> **症状**：上传成功、链接却打不开（404）。
> 这就是签名用一种风格、外链用另一种风格导致的。

---

## 五、在设置面板里手填

不想配环境变量，或临时试用：

设置 → **对象存储（图片/视频上传）** → 打开开关 → 选服务商预设 → 填：

| 项 | 说明 |
|---|---|
| Endpoint | 服务端点 |
| Region | 区域 |
| Bucket | 桶名 |
| 目录前缀 | 默认 `agnes-chat`，文件会存在这个"文件夹"下 |
| Access Key ID | 密钥 |
| Secret Access Key | 密钥 |
| 公开访问域名 | 可选，留空则用 endpoint 拼 |

填完点保存。**存在浏览器本地，换设备要重填**（登录 + 开云端保存会自动同步）。

---

## 六、排查

### 先看看诊断

访问 `/api/health`，看 `objectStorage` 这段：

```jsonc
{
  "objectStorage": {
    "kind": "b2",        // none = 没配好
    "siteManaged": true, // true = 用服务端环境变量
    "bucket": "agnes-chat"
  }
}
```

`kind` 是 `none` 说明一个配置都没读到。

### 常见问题

**❌ 上传报 403**

- 密钥填错了 → 重新生成一个 Application Key
- 桶的权限没开 **Read and Write** → 回去检查第 3 步

**❌ 上传成功但图片打不开**

- 桶没设成 **Public** → 桶设置里改
- `B2_PUBLIC_BASE_URL` 拼错了 → 用浏览器直接访问那个 URL 试试
- 末尾多了斜杠 → 去掉

**❌ 浏览器控制台报 CORS 错误**

桶的 CORS 规则没配，回去看第 5 步。

**❌ 配了环境变量还是不生效**

改环境变量后**必须重新部署**。Vercel 在 Deployments 里点 Redeploy。

**❌ AI 说看不到图片**

- 图片走了 URL 但外链不通 → 见上面"打不开"
- 模型本身不支持视觉（比如某些纯文本模型）→
  在自定义供应商里把它的「支持识图」取消勾选

**❌ 视频上传后播不了**

浏览器原生只认 mp4 / webm / ogg。wmv、avi、mpg 这些会走
浏览器内转码（ffmpeg wasm），首次点播要从 CDN 加载约 25MB。

---

## 七、免费额度对照

| 服务 | 免费存储 | 免费出站 | 单文件上限 |
|---|---|---|---|
| **Cloudflare R2** | 10 GB | **无限（零费用）** | 5 GB |
| **Backblaze B2** | 10 GB | 1 GB/天 | 无硬限制 |
| **Supabase** | 1 GB | 5 GB/月 | **50 MB** |
| AWS S3 | 5 GB（12 个月） | 100 GB/月（12 个月） | 5 GB |

个人聊天站，R2 和 B2 基本用不完。
Supabase 存储偏小、单文件上限也低，胜在和数据库统一后台。

---

## 八、允许上传什么

图片：`png jpg jpeg gif webp avif bmp svg ico heic`
视频：`mp4 webm mov m4v avi mkv ogv`

单个上限：图片 10MB、视频 100MB、其他 20MB。
