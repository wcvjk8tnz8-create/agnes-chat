# R2 对象存储配置教程

> 目标：**不手动**。填几个变量 → 推送 → 完事。
> 桶自动创建、自动绑定、自动可用，全程不需要 Access Key / Secret Key。

---

## 零、先搞清楚一件事

R2 桶是**你自己的**。Worker 通过 binding 访问它时，权限来自 binding 本身 ——
**不需要任何 Access Key / Secret Key**。

只有在**非 Workers 环境**（比如 Vercel 上远程访问 R2）才需要 AK/SK，
因为那里没有 binding，只能走 S3 兼容 API。

| 部署方式 | 需要 AK/SK 吗 | 需要 API 令牌吗 |
|---|---|---|
| **Cloudflare Workers（界面部署）** | ❌ | ❌ |
| **Cloudflare Workers（Actions 部署）** | ❌ | 建桶时用到一次 |
| Vercel / Netlify 远程访问 R2 | ✅ | ⭕ 可选（用于自动查账户 ID） |

---

## 一、界面部署（推荐，最省事）

### 第 1 步：确认配置里已经写好 binding

`wrangler.jsonc` 里默认就有这段，**一般不用改**：

```jsonc
"r2_buckets": [
  {
    "binding": "R2",
    "bucket_name": "agnes-chat"
  }
]
```

想换桶名就改 `bucket_name`，比如 `my-uploads`。

### 第 2 步：建一次桶

只需一次，之后永久有效：

```bash
npx wrangler r2 bucket create agnes-chat
```

> 已经建过会报 `code 10004`（桶已存在）—— **这不是错误，是成功**。

**不想装 Node？** 用 Dashboard：
Cloudflare 后台 → **R2** → **创建存储桶** → 填 `agnes-chat` → 创建。

### 第 3 步：连 Git 部署

Cloudflare 后台 → **Workers 和 Pages** → **创建** → 选 Workers → 连 GitHub

| 配置项 | 值 |
|---|---|
| 仓库 | `wcvjk8tnz8-create/agnes-chat` |
| 分支 | `main` |
| **Build command** | `npm run cf:build` |
| Deploy command | `npx wrangler deploy`（默认） |

保存 → 自动开始构建。

### 第 4 步：加两个密钥

Worker → **设置** → **变量和机密** → 添加：

| 名称 | 值 | 类型 |
|---|---|---|
| `PRESET_AGNES_API_KEY` | 你的 Agnes Key | 加密 |
| `SESSION_SECRET` | `openssl rand -base64 32` 生成 | 加密 |

⚠️ **加完要重新部署一次**才生效（Deployments → 重新部署）。

### 完成 ✅

不需要在网站设置里填任何东西。传张图试试，应该直接成功。

---

## 二、Actions 部署

### 第 1 步：填 Secrets

仓库 → **Settings** → **Secrets and variables** → **Actions**

| Secret | 必填 | 说明 |
|---|---|---|
| `CLOUDFLARE_API_TOKEN` | ✅ | 建桶、部署用 |
| `CLOUDFLARE_ACCOUNT_ID` | ✅ | Dashboard 右侧栏 |
| `KV_NAMESPACE_ID` | ⭕ | 不用 KV/D1 存储时可省略 |
| `D1_DATABASE_ID` | ⭕ | 同上 |
| `SESSION_SECRET` | ✅ | `openssl rand -base64 32` |
| `PRESET_AGNES_API_KEY` | ✅ | 站点内置 Key |

**可选 Variables**（Settings → Secrets and variables → Actions → Variables）：

| Variable | 默认值 | 说明 |
|---|---|---|
| `R2_BUCKET_NAME` | `agnes-chat` | 想改桶名就填 |

> 注意：Secrets 和 Variables 是**两个不同的标签页**，`R2_BUCKET_NAME` 填在 Variables 里。

### 第 2 步：推送或手动触发

推代码会自动跑；也可以去 **Actions** → 选工作流 → **Run workflow**。

### 第 3 步：脚本自动做的事

工作流里的 `scripts/ensure-r2.mjs` 会：

1. 调 API 列出账户下的桶
2. 目标桶在 → 直接用；不在 → 创建
3. **确保 `wrangler.jsonc` 里有 R2 binding**
4. 桶确实不可用 → 移除 binding，让部署能继续（降级为无对象存储）

第 4 步很关键：**如果桶不存在、配置里却留着 binding，`wrangler deploy` 会直接报
`bucket not found` 让整个部署失败**。宁可没存储，也不能让部署挂掉。

日志里会看到：

```
✅ R2 桶已存在：agnes-chat
✅ 已绑定 R2：binding "R2" → bucket "agnes-chat"
```

---

## 三、为什么以前"自动寻找"会说没有 API key

**这是我之前的设计失误，已修复。**

原来的逻辑是：点「自动寻找」→ 调 Cloudflare API 列桶 → 需要令牌 → 没配就报错。

问题是 **binding 模式下根本不需要找**：桶已经绑好了，Worker 直接就能读写，
跟 API 令牌毫无关系。用户点了按钮却被告知"没有 API key"，纯属自找麻烦。

现在改成三级：

| 级别 | 条件 | 行为 |
|---|---|---|
| 1 | **有 R2 binding** | 直接用，零配置 ✅ |
| 2 | 有 API 令牌 | 调 API 按名字匹配桶 |
| 3 | 都没有 | 明确告诉用户下一步怎么做 |

**所以：如果你用了 binding，根本不用点「自动寻找」，上传直接就能用。**

---

## 四、验证

部署后：

1. 打开网站 → 设置 → 对象存储
   - 看到「已通过 Worker 绑定直连 R2」→ ✅ 完成
2. 直接传张图片 → 成功且能预览 → ✅ 完成
3. 管理员访问 `/api/health` → `objectStorage.siteManaged: true`

| 现象 | 原因 | 处理 |
|---|---|---|
| 提示"未启用对象存储" | 没绑桶 | 检查 `wrangler.jsonc` 的 `r2_buckets` |
| 上传报 503 | binding 缺失 | 重新部署一次 |
| 图片发出去 AI 看不到 | 读取路由问题 | 见下方说明 |

> 图片读取走 `/api/r2/<key>`，所以**桶不需要开公开读**。
> 配了 `S3_ACCESS_HOST` 自定义域时会直接用该域名。

---

## 五、可选：自定义域名

不想用默认的 `/api/r2/` 代理路径，可以绑自定义域：

1. R2 桶 → **设置** → **公开访问** → 连接域
2. 填一个已托管在 Cloudflare 的域名，如 `images.example.com`
3. 添加环境变量 `S3_ACCESS_HOST=https://images.example.com`

之后上传的文件会用这个域名生成链接，不再走代理。

---

## 六、非 Workers 环境（Vercel / Netlify）

这些平台没有 binding，只能走 S3 兼容 API：

1. Cloudflare 后台 → R2 → **管理 R2 API 令牌** → 创建 API 令牌
   （权限选「对象读和写」）
2. 会得到 Access Key ID 和 Secret Access Key
3. 填环境变量：

```bash
R2_BUCKET_NAME=agnes-chat
R2_ACCESS_KEY_ID=xxx
R2_SECRET_ACCESS_KEY=xxx
R2_ACCOUNT_ID=你的账户ID       # 32 位十六进制
S3_ACCESS_HOST=https://pub-xxx.r2.dev   # 可选，需先开公开读
```

> 💡 `R2_ACCOUNT_ID` 可以不填 —— 只要配了 `CLOUDFLARE_API_TOKEN`，
> 系统会调 `/accounts` 自动查出来。

---

## ⚠️ 最容易搞混的一点：GitHub Secrets ≠ 线上环境变量

**这是"明明配了却读不到"的头号原因。**

你在 GitHub 仓库 `Settings → Secrets and variables → Actions` 里填的那些
（`CLOUDFLARE_API_TOKEN`、`CLOUDFLARE_ACCOUNT_ID`、`R2_BUCKET_NAME`…）
**只对 GitHub Actions 生效** —— 它们是给构建流程用的，
一旦部署完成，运行中的站点**根本读不到**。

| 填在哪 | 谁能读到 |
|---|---|
| GitHub 仓库 Secrets | 只有 GitHub Actions（构建时） |
| Cloudflare 后台 → Workers → 设置 → 变量和机密 | **运行中的站点** ✅ |
| `wrangler secret put` | 运行中的站点 ✅ |
| Vercel / Netlify 项目设置 → Environment Variables | 运行中的站点 ✅ |

所以如果 Actions 构建成功了，但打开网站还是报"缺少令牌"，
说明线上环境变量没配 —— 去 Cloudflare 后台再写一遍。

**不过更省事的是：绑定 R2 桶之后根本不需要令牌。**
`CLOUDFLARE_API_TOKEN` 只在「自动建桶 / 自动查账户 ID」时才用得上，
绑定已经存在的情况下，网站运行期间完全不需要它。

---

## 常见问题

**Q：真的什么都不用填就能上传吗？**
A：在 Workers 上、且 `wrangler.jsonc` 里绑了桶 —— 是的，零配置。
网站的「设置 → 对象存储」里不用动任何东西。

**Q：那「自动寻找」按钮还有用吗？**
A：只有你想**改用另一个桶**、且走 S3 兼容 API 时才用得上。
binding 模式下点了也没关系，会直接告诉你"已直连，无需密钥"。

**Q：桶名一定要叫 `agnes-chat` 吗？**
A：不用。改 `wrangler.jsonc` 的 `bucket_name`，或设 `R2_BUCKET_NAME` 变量。
但**桶必须先建好**，binding 不会自动创建桶。

**Q：文件有大小限制吗？**
A：binding 直传受 Workers 请求体上限约束（免费版 100MB）。
超大文件需要走预签名直传（R2 单次 PUT 上限 5GB）。

**Q：视频能传吗？**
A：能。支持 mp4 / wmv / mpg / mpg2 / avi / flv / rmvb 等几十种格式。
浏览器播不了的（wmv 等）会**在浏览器里用内置解码器转成 MP4** 再播，
不消耗服务器资源。
